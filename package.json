{
  "name": "face-tracker",
  "version": "1.1.0",
  "description": "Async Node.js addon for face detection/landmarks using libfacedetection + OpenCV",
  "main": "index.js",
  "gypfile": true,
  "scripts": {
    "build": "node-gyp configure build",
    "rebuild": "node-gyp rebuild",
    "install": "node-gyp rebuild",
    "dev": "node -e \"console.log('Run your test file, e.g. node example.js')\""
  },
  "dependencies": {
    "bindings": "^1.5.0",
    "node-addon-api": "^7.1.0",
    "sharp": "^0.33.0"
  },
  "devDependencies": {
    "node-gyp": "^10.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
